Unbound container for docker and podman
=======================================

use the sample compose.yaml to run the container

overide the unbound.conf by adding a .conf file to an etc-unbound directory in your project and uncomment the appropriate section from the compose.yaml
